[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm install --omit=dev", "pip install --break-system-packages youtube-transcript-api"]

[phases.build]
cmds = ["echo 'Backend only - no frontend build needed'"]

[start]
cmd = "node server-clean.mjs"
