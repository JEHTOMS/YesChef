{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd food-ai && rm -rf node_modules && npm ci --omit=dev && python3 -m pip install --no-cache-dir youtube-transcript-api"
  },
  "deploy": {
    "startCommand": "cd food-ai && node server-clean.mjs",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 180
  }
}
